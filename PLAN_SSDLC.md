# kafka_DDD_msg

@AGENTS.md

Each `appNN_*/` directory is a self-contained app. app01_java_kafka_msg's
SSDLC plan lives in `app01_java_kafka_msg/PLAN_SSDLC.md` — read it before
touching app01 code; don't duplicate it here.
